{
  "name": "Playwright + Streamlit",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.shell.linux": "/bin/bash",
        "python.defaultInterpreterPath": "/workspaces/replit/venv/bin/python"
      },
      "extensions": [
        "ms-python.python",
        "ms-playwright.playwright"
      ]
    }
  },
  "postCreateCommand": "python -m venv venv && source venv/bin/activate && pip install --upgrade pip && pip install -r requirements.txt",
  "forwardPorts": [5000],
  "remoteUser": "vscode",
  "containerEnv": {
    "HOME": "/home/vscode"
  },
  "onCreateCommand": "sudo mkdir -p /home/codespace && sudo chown vscode:vscode /home/codespace && mkdir -p /home/codespace/.config/vscode-dev-containers"
}
